<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.desenvappmoveis.n1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".showResultReprovado"
            android:label="Resultado"/>
        <activity android:name=".editNotas"
            android:label="Editar Aluno" />
        <activity android:name=".showResultAprovado"
            android:label="Resultado"/>
        <activity android:name=".editNota2"
            android:label="Informar Segunda Nota"/>
        <activity android:name=".editNota1"
            android:label="Informar Primeira Nota"/>
        <activity android:name=".insertAluno"
                  android:label="Adicionar Aluno"
            />

        <activity
            android:name=".MainActivity"
            android:label="N1"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>